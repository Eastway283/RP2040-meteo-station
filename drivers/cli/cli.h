#ifndef CLI_H
#define CLI_H

// Запускает интерактивный сеанс командной строки.
// Функция блокируется до тех пор, пока пользователь не введёт команду
// и не нажмёт Enter. После выполнения команды управление возвращается.
// Вызывайте её, только если обнаружено нажатие клавиши (чтобы не блокировать
// основной цикл измерений).
void cli_process(void);

#endif // CLI_H
